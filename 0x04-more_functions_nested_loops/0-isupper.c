#include "main.h"
#include <ctype.h>
/**
 * _isupper - check upper character
 * Return: i if upper 0 otherwise
 *@c: paramter of charcter
 */
int _isupper(int c)
{
	if (isupper(c))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
