#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 *@num: the integer paramter
 * Return: last Digit
 */
int print_last_digit(int num)
{
	int digit;

	digit = num % 10;
	return (digit);
}
