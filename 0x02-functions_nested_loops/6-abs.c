#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer
 *@num: integer number
 * Return: return 1 if success
 */
int _abs(int num)
{int abNum;
abNum = abs(num);
return (1);
}
