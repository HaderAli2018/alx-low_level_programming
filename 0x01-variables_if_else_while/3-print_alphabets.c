#include <stdio.h>
/**
 *  main -  Entry point
 *
 * Return: Always 0 if (Success)
 */
int main(void)
{
	char c = 'a', d = 'A';

	while (c <= 'z')
	{
		putchar(c);
	}
	while (d < = 'Z')
	{
		putchar(d);
	}
	putchar('\n');
	return (0);
}
