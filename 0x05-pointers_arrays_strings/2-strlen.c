#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: character paramter
 * Return: int value
 */
int _strlen(char *s)
{
	int length;

	length =  strlen(s);
	return (length);
}
